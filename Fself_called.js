// Self-invoked function 

(function ()
{
    let x =Number(prompt("Enter a value : "));

    if(x>0)
    {
        console.log(x," is Positive  value .");
    }
    else{
        console.log(x," is Negative value .");
    }
})();